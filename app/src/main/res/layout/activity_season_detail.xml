<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/tv_background"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/tv_margin_large">

        <!-- 📱 左侧海报 -->
        <ImageView
            android:id="@+id/season_poster"
            android:layout_width="@dimen/tv_poster_width"
            android:layout_height="@dimen/tv_poster_height"
            android:layout_marginEnd="@dimen/tv_margin_large"
            android:scaleType="centerCrop"
            android:background="@drawable/bg_card"
            android:contentDescription="@string/poster_description" />

        <!-- 📝 右侧内容 -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- 标题 -->
            <TextView
                android:id="@+id/season_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="@dimen/tv_text_size_title"
                android:textColor="@color/tv_text_primary"
                android:fontFamily="@font/tv_font_bold"
                tools:text="电视剧标题" />

            <!-- 副标题（季信息） -->
            <TextView
                android:id="@+id/season_subtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/tv_margin_small"
                android:textSize="@dimen/tv_text_size_medium"
                android:textColor="@color/tv_text_secondary"
                tools:text="第 1 季 · 2023" />

            <!-- 播放按钮 -->
            <TextView
                android:id="@+id/season_play_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/tv_margin_large"
                android:paddingHorizontal="@dimen/tv_margin_large"
                android:paddingVertical="@dimen/tv_margin_medium"
                android:text="▶ 播放第1集"
                android:textSize="@dimen/tv_text_size_large"
                android:textColor="@color/tv_text_on_accent"
                android:background="@drawable/bg_button_primary"
                android:clickable="true"
                android:focusable="true" />

            <!-- 简介 -->
            <TextView
                android:id="@+id/season_summary"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/tv_margin_large"
                android:textSize="@dimen/tv_text_size_medium"
                android:textColor="@color/tv_text_primary"
                android:maxLines="4"
                android:ellipsize="end"
                tools:text="剧集简介..." />

            <!-- 📺 剧集列表区域 -->
            <LinearLayout
                android:id="@+id/episode_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/tv_margin_large"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/episode_list_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="剧集列表"
                    android:textSize="@dimen/tv_text_size_large"
                    android:textColor="@color/tv_text_primary"
                    android:fontFamily="@font/tv_font_medium" />

                <!-- 📑 剧集分组标签 (1-30 / 31-60 / 61-73) -->
                <HorizontalScrollView
                    android:id="@+id/episode_group_scroll"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/tv_margin_medium"
                    android:scrollbars="none"
                    android:visibility="gone">

                    <LinearLayout
                        android:id="@+id/episode_group_tabs"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal" />

                </HorizontalScrollView>

                <!-- 📺 横向剧集卡片列表 -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/episode_recycler_view"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/tv_margin_medium"
                    android:clipToPadding="false"
                    android:nestedScrollingEnabled="false" />

            </LinearLayout>

            <!-- 👥 演职人员区域 -->
            <LinearLayout
                android:id="@+id/person_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/tv_margin_xlarge"
                android:orientation="vertical"
                android:visibility="gone">

                <!-- 导演 -->
                <LinearLayout
                    android:id="@+id/director_section"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:visibility="gone">

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="导演"
                        android:textSize="@dimen/tv_text_size_medium"
                        android:textColor="@color/tv_text_primary"
                        android:fontFamily="@font/tv_font_medium" />

                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/director_recycler_view"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/tv_margin_small"
                        android:orientation="horizontal" />

                </LinearLayout>

                <!-- 演员 -->
                <LinearLayout
                    android:id="@+id/actor_section"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/tv_margin_medium"
                    android:orientation="vertical"
                    android:visibility="gone">

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="演员"
                        android:textSize="@dimen/tv_text_size_medium"
                        android:textColor="@color/tv_text_primary"
                        android:fontFamily="@font/tv_font_medium" />

                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/actor_recycler_view"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/tv_margin_small"
                        android:orientation="horizontal" />

                </LinearLayout>

                <!-- 编剧 -->
                <LinearLayout
                    android:id="@+id/writer_section"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/tv_margin_medium"
                    android:orientation="vertical"
                    android:visibility="gone">

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="编剧"
                        android:textSize="@dimen/tv_text_size_medium"
                        android:textColor="@color/tv_text_primary"
                        android:fontFamily="@font/tv_font_medium" />

                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/writer_recycler_view"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/tv_margin_small"
                        android:orientation="horizontal" />

                </LinearLayout>

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</ScrollView>
