<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="bottom">

    <!-- 🎮 播放控制器底部栏 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/tv_margin_medium"
        android:background="@drawable/gradient_bottom_overlay">

        <!-- 📊 进度条 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="@dimen/tv_margin_small">

            <TextView
                android:id="@id/exo_position"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/white"
                android:textSize="@dimen/tv_text_size_small"
                android:fontFamily="@font/tv_font_regular"
                android:text="00:00" />

            <androidx.media3.ui.DefaultTimeBar
                android:id="@id/exo_progress"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginHorizontal="@dimen/tv_margin_medium" />

            <TextView
                android:id="@id/exo_duration"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/white"
                android:textSize="@dimen/tv_text_size_small"
                android:fontFamily="@font/tv_font_regular"
                android:text="00:00" />

        </LinearLayout>

        <!-- 🎮 控制按钮 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center">

            <!-- 快退按钮 -->
            <ImageButton
                android:id="@id/exo_rew"
                android:layout_width="@dimen/tv_button_size"
                android:layout_height="@dimen/tv_button_size"
                android:background="@drawable/tv_button_background"
                android:src="@drawable/ic_play_arrow"
                android:rotation="180"
                android:tint="@color/white"
                android:contentDescription="快退"
                android:focusable="true"
                android:layout_marginEnd="@dimen/tv_margin_medium" />

            <!-- 播放/暂停按钮 -->
            <ImageButton
                android:id="@id/exo_play_pause"
                android:layout_width="@dimen/tv_button_size_large"
                android:layout_height="@dimen/tv_button_size_large"
                android:background="@drawable/tv_button_background"
                android:src="@drawable/ic_play_arrow"
                android:tint="@color/white"
                android:contentDescription="播放/暂停"
                android:focusable="true"
                android:layout_marginHorizontal="@dimen/tv_margin_medium" />

            <!-- 快进按钮 -->
            <ImageButton
                android:id="@id/exo_ffwd"
                android:layout_width="@dimen/tv_button_size"
                android:layout_height="@dimen/tv_button_size"
                android:background="@drawable/tv_button_background"
                android:src="@drawable/ic_play_arrow"
                android:tint="@color/white"
                android:contentDescription="快进"
                android:focusable="true"
                android:layout_marginStart="@dimen/tv_margin_medium" />

            <!-- 弹幕设置按钮 -->
            <ImageButton
                android:id="@+id/btn_danmu_settings"
                android:layout_width="@dimen/tv_button_size"
                android:layout_height="@dimen/tv_button_size"
                android:background="@drawable/tv_button_background"
                android:src="@drawable/ic_settings"
                android:tint="@color/white"
                android:contentDescription="弹幕设置"
                android:focusable="true"
                android:layout_marginStart="@dimen/tv_margin_medium" />

        </LinearLayout>

        <!-- 📊 附加信息 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="@dimen/tv_margin_small">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Android TV播放器"
                android:textColor="@color/tv_text_secondary"
                android:textSize="@dimen/tv_text_size_small"
                android:fontFamily="@font/tv_font_regular" />

            <TextView
                android:id="@+id/osd_danmu_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="@dimen/tv_margin_medium"
                android:text="弹幕: 开启"
                android:textColor="@color/tv_accent_primary"
                android:textSize="@dimen/tv_text_size_small"
                android:fontFamily="@font/tv_font_bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="按返回键退出"
                android:textColor="@color/tv_text_tertiary"
                android:textSize="@dimen/tv_text_size_small"
                android:fontFamily="@font/tv_font_regular" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
